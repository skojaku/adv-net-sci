# Dependencies of lecture-note/figs/src/*.py, driven by tools/build_figures.sh.
# Deliberately minimal: the lecture note itself executes no code, so this is the
# only Python CI needs to render the site.
igraph
matplotlib
networkx
numpy
pandas
pillow
powerlaw
requests
seaborn
