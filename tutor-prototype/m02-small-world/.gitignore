# NOTE what is NOT here: notebook.py, session_artifacts/, assets/uploads/ and
# assets/exercises/ are your work — the notebook, the log, your photographs and
# the code you wrote. They are what you submit, so they are meant to be
# committed. `git add -A` picks them all up.

# Fetched, never edited: the toolkit pi installs from .pi/settings.json.
.pi/git/
.pi/npm/
.pi/skills/
.pi/marimo-bridge/
.skill-cache/
.claude/skills/

# marimo's own scratch directory.
__marimo__/

__pycache__/
*.py[cod]
.DS_Store
