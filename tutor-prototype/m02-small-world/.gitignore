__marimo__/
.skill-cache/
.pi/skills/
.claude/skills/
session_artifacts/
notebook.py
# Student photo uploads live WITH the notebook (nb_view_image writes here so
# the keepsake still renders when session_artifacts/ is left behind), but they
# are session data, not module source.
assets/uploads/
# Same for the code a student writes in an exercise box: their work, saved
# beside the notebook so the keepsake still renders it, but not module
# source. A solved cp6 committed here is an answer key in the repo.
assets/exercises/
