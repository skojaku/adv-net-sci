# The live alias -> real-model mapping. This repository is public; committing
# the real config would publish exactly the thing the gateway exists to hide.
config.yaml
config.*.yaml
!config.example.yaml

# Secrets and state, both of which live only on the VM.
.env
*.db
*.db-wal
*.db-shm
issued-keys*.csv

.venv/
__pycache__/
*.pyc
.pytest_cache/
