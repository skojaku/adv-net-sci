---
title: "Hands-on: Turning a Network into Coordinates"
---

Two families of embedding, built from scratch, then judged on the same task.

::: {.callout-note title="Where the code lives"}
This course keeps its lecture note free of runnable code. Everything you
execute lives in a marimo notebook, so it stays interactive and reactive
instead of being a wall of output frozen into a web page.

[Open the notebook in molab]({{< var molab.m08 >}})

Prefer to work locally? The same notebook is in the repository at
[`notebooks/m08-network-embedding/coding.ipynb`](https://github.com/skojaku/adv-net-sci/blob/main/notebooks/m08-network-embedding/coding.ipynb).
Run it with `marimo edit notebooks/m08-network-embedding/coding.py`.
:::

## What you will build

1. Implement Laplacian eigenmap and PCA on the adjacency matrix, and plot both.
2. Generate biased random walks with node2vec's p and q parameters.
3. Train a skip-gram model on those walks.
4. Factorize network matrices directly and compare with the neural embeddings.
5. Evaluate everything by link prediction.
6. Visualize higher-dimensional embeddings with t-SNE.

## What to watch for

- Sweep q from below 1 to above 1 and watch which nodes end up near each other. Low q should recover communities, high q should group nodes by role.
- Train the same model twice. The coordinates will differ completely while the link prediction score barely moves — embeddings are only defined up to rotation.
