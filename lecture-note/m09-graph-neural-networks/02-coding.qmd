---
title: "Hands-on: Building Graph Neural Networks"
---

GCN, GraphSAGE and GAT on the same task, plus the failure mode that depth causes.

::: {.callout-note title="Where the code lives"}
This module has no hosted notebook. PyTorch is a compiled package that the
browser runtime cannot load, so this one runs locally only.

The notebook is in the repository at
[`notebooks/m09-graph-neural-nets/coding.py`](https://github.com/skojaku/adv-net-sci/blob/main/notebooks/m09-graph-neural-nets/coding.py).
Run it with `marimo edit notebooks/m09-graph-neural-nets/coding.py` in an
environment that has PyTorch and PyTorch Geometric installed.
:::

## What you will build

1. Set up PyTorch Geometric and turn a network into a Data object with node features.
2. Implement GCN, GAT and GraphSAGE and train them for node classification.
3. Compare their accuracy on the same train/validation split.
4. Build a link prediction model on top of the learned representations.
5. Assemble a graph classification model with a readout layer.
6. Visualize the learned node embeddings, and the attention weights for GAT.

## What to watch for

- Train GCNs with 2, 4, 8 and 16 layers. Accuracy will peak early and then fall — plot it, because over-smoothing is much more convincing when you have caused it yourself.
- Replace the node features with all-ones and re-run. Whatever accuracy remains came from topology alone.

## Also in this module

**Image processing preliminaries** — Edge detection, kernels, and the Fourier transform worked through on real images — the concrete version of the theory in the preparation chapter.

[Source](https://github.com/skojaku/adv-net-sci/blob/main/notebooks/m09-graph-neural-nets/image-processing.ipynb) — run locally.

**A GCN from scratch** — Bruna's spectral GCN and ChebNet implemented directly from the equations, without PyTorch Geometric's layers.

[Source](https://github.com/skojaku/adv-net-sci/blob/main/notebooks/m09-graph-neural-nets/gcn-from-scratch.ipynb) — run locally.