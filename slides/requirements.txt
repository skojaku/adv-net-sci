# Dependencies of slides/<module>/figures/*.py.
# The figure scripts also shell out to LaTeX and need the `standalone` class:
#     tlmgr --usermode install standalone
matplotlib
networkx
numpy
pillow
